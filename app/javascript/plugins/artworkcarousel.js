const artworkCarousel = () => {
  const oeuvre = document.querySelector('#0')
  const modal = document.querySelector('.carousel-item')

  oeuvres.addEventListener('click', (e) => {
    const index = @i
    if (e.target.classList.contains('fas')) {
          e.target.classList.add('active');
  });
}
export { artworkCarousel };
